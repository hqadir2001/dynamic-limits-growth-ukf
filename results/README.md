# Results

Generated model outputs are intentionally not committed by default. Run the notebook or scripts to generate run-specific artifacts locally under this directory.

Keep large run folders, ZIP packs, logs, and intermediate files out of Git unless you explicitly promote a small, publication-facing subset.
